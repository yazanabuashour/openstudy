# OpenStudy v0.1.0

## Changed

- First public release of the local-first `openstudy` JSON runner and single-file agent skill.
- Includes card management, source pointer attachment, review sessions, answer recording, due-window inspection, local SQLite storage, and FSRS-backed scheduling through the runner-mediated surface.
- Ships platform runner archives, the skill archive, source archive, `openstudy_0.1.0_checksums.txt`, SBOM, installer, and GitHub release attestations.

## Verification

- Release-blocking production eval report: [docs/evals/results/os7nh-v0.1.0.md](../evals/results/os7nh-v0.1.0.md).
- Production eval model pin: `gpt-5.4-mini`.
- Local gates: `git diff --check`, `go test ./...`, skill validation, committed-artifact validation, and release-doc validation.
- Artifact verification: checksum validation with `openstudy_0.1.0_checksums.txt`, source archive hygiene checks, skill archive listing, install smoke test, and GitHub attestations.
